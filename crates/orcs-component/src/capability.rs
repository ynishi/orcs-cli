//! Re-export of [`Capability`] from `orcs-auth`.
//!
//! The canonical definition lives in [`orcs_auth::capability`].
//! This module re-exports it for backward compatibility.

pub use orcs_auth::Capability;
